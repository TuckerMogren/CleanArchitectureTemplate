namespace Worker
{
    /// <summary>
    /// Marker type to reference the Domain assembly.
    /// </summary>
    public sealed class Marker
    {
        private static readonly System.Type? _0 = typeof(Application.Marker);
        private static readonly System.Type? _1 = typeof(Domain.Marker);
        private static readonly System.Type? _2 = typeof(Infrastructure.Marker);
        private static readonly System.Type? _3 = typeof(Infrastructure.Persistence.Marker);
    }
}
